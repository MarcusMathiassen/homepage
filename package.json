{
    "browserslist": [
        "last 1 chrome versions"
    ],
    "scripts": {
        "dev": "rm -rf ./docs && parcel src/index.pug -d docs",
        "build": "rm -rf ./docs && parcel build --no-source-maps src/index.pug -d docs",
        "deploy": "npm run build && echo www.mathiassen.dev > docs/CNAME"
    },
    "devDependencies": {
        "bulma": "^0.8.0",
        "coffeescript": "^2.4.1",
        "glslify-bundle": "^5.1.1",
        "glslify-deps": "^1.3.1",
        "node-sass": "^4.13.1",
        "parcel-bundler": "^1.12.4",
        "parcel-plugin-purgecss": "^2.1.3",
        "parcel-plugin-svelte": "^4.0.5",
        "pug": "^2.0.4",
        "sass": "^1.23.7",
        "svelte": "^3.16.4",
        "svelte-preprocess": "^3.2.6"
    },
    "dependencies": {
        "netlify-cli": "^2.30.0",
        "node-fetch": "^2.6.0"
    }
}
